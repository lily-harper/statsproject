load_libraries <- function() {
    library(ggplot2)
    library(tidyverse)
    library(stargazer)
    library(lubridate)
    library(patchwork)
    library(zoo)
    library(modelsummary)
    library(fixest)
    library(fixest)
    library(modelsummary)
}